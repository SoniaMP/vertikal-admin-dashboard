# SET — Configuración y branding

Almacén clave-valor de la configuración global del club: temporada activa, cuota de membresía, branding de email (logo y colores), listas de destinatarios de notificaciones. Toda esta configuración vive en la tabla `AppSetting`.

## Reglas

- Existe una y solo una temporada activa en cada momento; el resto del sistema la resuelve vía `getActiveSeason()` en `src/lib/settings.ts`.
- La cuota de socio (`MEMBERSHIP_FEE`) se aplica a todas las membresías al rate vigente en el momento del alta y se snapshottea en `Membership.socioFeeSnapshot` (ver [MEM](00.memberships.md)).
- El logo del email se sirve con URL absoluta (compatibilidad con clientes de correo externos).
- Las listas de destinatarios de notificaciones se almacenan como JSON serializado en `AppSetting`; lista vacía → se omiten las notificaciones de ese tipo (ver [NOTIF](06.notifications.md)).

## Casos de uso

(No hay UCs específicos de SET; son operaciones admin sencillas de form submission.)

## Backlog

### Temporada como año entero con transición seca

Hoy `Season.name: String` (ej. "2025-2026") es ambiguo: el orden lexicográfico no siempre coincide con el cronológico, las comparaciones requieren parsing, y el flag `isActive` se marca a mano por el admin. Decisión: renombrar a `Season.year: Int @unique` (un año = una temporada). `startDate` y `endDate` se derivan a 16 dic / 15 dic. La transición es seca (sin overlap) el 16 de diciembre. `getActiveSeason()` resuelve por fecha (no por flag); el campo `isActive` desaparece de la UI admin.

- [ ] Añadir `Season.year: Int @unique` al schema; backfill parseando "YYYY-YYYY" al año superior (o destruir, pre-prod).
- [ ] Backfill `startDate` (16 dic año-1) y `endDate` (15 dic año) si no están alineados.
- [ ] Drop columna `name`.
- [ ] Actualizar todas las queries y rendering que referencian `Season.name`.
- [ ] Reescribir `getActiveSeason()`: query por `WHERE startDate <= now() AND endDate >= now()`. Error claro si hay 0 o >1 coincidencias.
- [ ] Ocultar `isActive` de la tabla admin de temporadas y del form (queda como dead column).
- [ ] Tests: migración, queries, error en boundary cases.

### Banner público con la temporada activa

En los flujos públicos (alta, renovación, inscripción a curso) hoy no se ve para qué temporada se está registrando el usuario. Añadir un banner "Temporada {year}" que persiste a través de los pasos del wizard, para evitar que alguien se inscriba a la temporada equivocada.

- [ ] Componente `<SeasonBanner />` con texto "Temporada {year}".
- [ ] Render en `/registro/alta`, `/registro/renovacion` y wizard de inscripción a curso.
- [ ] Persiste a través de los pasos del wizard (no se desmonta al cambiar de step).
- [ ] Tests de visibilidad en cada flujo público.
